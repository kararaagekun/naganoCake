<div class='container'>
  <div class='row'>
    <div class='col-md-6'>
      <h2>ジャンル編集</h2><br>
      <%= form_with model: @genre, url: admins_genre_path do |f| %>
      <%= f.label :ジャンル %><%= f.text_field :name %>
      <%= f.submit '変更を保存', class: "btn btn-success" %>
      <% end %>
    </div>
  </div>
</div>