<div class="container mt-5">
  <div class="row">
    <div class="col3">
      <h2 class="bg-light text-black">ジャンル編集</h2>
    </div>
  </div>
</div>

<div class='container mt-4'>
  <div class='row'>
    <div class='col-md-6'>
      <%= form_with model: @genre, url: admins_genre_path do |f| %>
      <%= f.label :ジャンル %><%= f.text_field :name %>
      <%= f.submit '変更を保存', class: "btn btn-success" %>
      <% end %>
    </div>
  </div>
</div>